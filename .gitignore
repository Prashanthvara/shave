# python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# node / worker
node_modules/
.wrangler/
dist/

# secrets
.dev.vars
.env
*.env.local

# pipeline artifacts — large, regenerable, never committed
data/raw/
data/interim/
*.gdb/
*.gpkg
*.parquet
!tests/fixtures/*.parquet
*.zip

# tooling noise
.playwright-mcp/
.DS_Store
.claude/settings.local.json

# local-only Claude Code settings
.claude/settings.local.json

# hand-authored domain data, not a build artifact
!data/occupants.csv

# site: the page source is tracked, every generated artifact is not.
# This block SUPERSEDES the `public/*` + `!public/ranked.json` rules that
# first-ranked-list Task 5 step 10 specifies. Applied in sequence those would
# hide public/index.html and the deploy would serve nothing.
node_modules/
.wrangler/
public/data/
public/ranked.json
docs/
.claude/
CLAUDE.md
